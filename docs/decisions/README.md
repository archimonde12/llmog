# Decisions

The `docs/decisions` directory stores important technical decisions for the project.

## Structure
- `README.md`: overview + decision map
- `001_{decision_name}.md`, `002_{decision_name}.md`, ...: one file per decision

## Naming Convention
- Use an incremental 3-digit prefix.
- Use English file names in snake_case.
